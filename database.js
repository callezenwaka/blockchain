'use strict';

const _transactions = [];
const _balances = [];
// const items = [1,2,3];
const transactions = Promise.all([]);
const balances = Promise.all([]);

// Export All Methods
module.exports = {
  transactions,
  balances,
}