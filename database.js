'use strict';

const transactions = Promise.all([]);
const balances = Promise.all([]);
const blocks = Promise.all([]);

// Export All Methods
module.exports = {
  transactions,
  balances,
  blocks,
}