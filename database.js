'use strict';

const _transactions = [];
const _balances = [];
const transactions = Promise.all([]);
const balances = Promise.all([]);

// Export All Methods
module.exports = {
  transactions,
  balances,
}